{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum desktop permissions for the employee agent shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
