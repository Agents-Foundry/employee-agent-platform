[package]
name = "agents-foundry-employee"
version = "0.1.0"
description = "Governed employee agent desktop shell"
authors = ["Agents Foundry"]
edition = "2021"

[lib]
name = "agents_foundry_employee_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
